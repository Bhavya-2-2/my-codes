name: Docker Build Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker image
        run: docker build -t bhavya-python-app .
      - name: Run container test
        run: docker run -d -p 5000:5000 bhavya-python-app

